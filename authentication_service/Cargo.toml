[package]
name = "authentication-service"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
akd = { version = "0.11.0", features = ["serde_serialization"] }
actix-rt = "2.0"
actix-web = "4"